"use strict";
/* Единственное место, где живут название, домен, почта и владелец.
   По шаблонам они не разбросаны: страницы берут всё отсюда, поэтому
   переименование или смена домена — правка одного файла. */

/* Заглушки прошлого этапа. Значения ушли, а константы остались:
   по ним линт проверяет, что в собранном dist ничего от них не уцелело. */
const PLACEHOLDER_ORIGIN = "https://example.org";
const PLACEHOLDER_MAIL = "oshibka@example.org";
const WORKING_TITLE = "Справочник о теле"; /* рабочее название до выбора имени */

module.exports = {
  origin: process.env.SITE_ORIGIN || "https://wikitelo.ru",

  /* Название и пояснение к нему. Пояснение объявляет назначение там,
     где само имя его не несёт: в шапке и на главной. */
  siteName: "Викитело",
  tagline: "справочник о теле",

  /* Кнопка «здесь ошибка» — mailto, а не форма: формы нет не из лени,
     а потому что поля «опишите вашу ситуацию» быть не должно. */
  errorMail: process.env.SITE_ERROR_MAIL || "wikitelo@mail.ru",

  /* Сведения о владельце. Реквизитов нет намеренно: пока нет рекламы
     и продаж, они не требуются. Появятся — добавлять сюда. */
  owner: {
    name: "Александр Иванов",
    mail: "wikitelo@mail.ru"
  },

  PLACEHOLDER_ORIGIN,
  PLACEHOLDER_MAIL,
  WORKING_TITLE
};
