# База знаний справочника

**База закрыта полностью: 110 разделов из 110, все десять областей тела.**

| | |
|---|---|
| Статей о состояниях | **307**, из них 100 «редко, но важно» |
| Разделов справочника | **110** — 54 первого приоритета, 46 второго, 10 третьего |
| Связей раздел → состояние | **1126** |
| Вопросов в общем пуле | **138** |
| Правил красных флагов | **60** |
| Якорей на модели тела | 52 участка, 137 связей |
| Тестовых случаев | **252**, проходят все |

248 статей из 307 работают больше чем в одном разделе. Опоясывающий лишай нужен в шестнадцати разделах, инфаркт в пятнадцати, анемия в пятнадцати — и каждая написана однажды.

## Файлы

| Файл | Что внутри |
|---|---|
| `schema.json` | Схема данных, версия 1.0.0, заморожена |
| `syndromes-map.json` | Оглавление: 10 областей, 110 разделов |
| `anatomy.json` | Слой якорей: точка на модели тела → раздел. Координаты в долях от габарита фигуры |
| `questions.json` | Общий пул из 138 признаков, переиспользуется всеми областями |
| `redflags.json` | 60 правил красных флагов + тексты «моего случая нет» и «что здесь не разбирают» |
| `conditions-*.json` | Статьи о состояниях: neck, head, back, general, abdomen, chest, pelvic, limbs, misc |
| `syndromes-*.json` | Разделы справочника со связями и весами |
| `vignettes-*.json` | 252 тестовых случая с известным ответом |
| `validate.js` | Проверка целостности. Запускать после любой правки |
| `engine.js` | Движок ранжирования + прогон тестов. Тот же код пойдёт на сайт |

## Как проверять

```
node validate.js    # целостность: битые ссылки, пустые поля, источники, якоря
node engine.js      # 252 тестовых случая
```

Оба должны заканчиваться без ошибок. Если после правки весов тест падает — неверны веса, а не тест.

## Как устроена база

Две отдельные полки:

- **Статьи о состояниях** (`conditions-*.json`) пишутся один раз. Пневмония нужна в девяти разделах, но существует в одном экземпляре.
- **Разделы справочника** (`syndromes-*.json`) текста о болезнях не содержат — только ссылки на статьи и веса признаков.

Поэтому наполнять «по областям» нельзя: получатся дубли, которые со временем разъедутся.

## Веса

Ключ веса — `идВопроса_идОтвета`, например `plaque_grey`.

| Значение | Смысл |
|---|---|
| +4…+6 | Патогномонично: почти не бывает при других состояниях |
| +2…+3 | Сильно за |
| +1 | За |
| −2…−3 | Против |
| −4…−6 | Почти исключает |

Ответ «не знаю» веса не имеет нигде — валидатор это проверяет и не пропустит.

**Стоп-факторы важнее больших отрицательных весов.** У кандидата может быть поле `stop` — список ответов, полностью исключающих состояние. Отрицательный вес перевешивается суммой положительных: удалённый аппендикс с весом −6 всё равно проигрывал восьми совпавшим признакам аппендицита. Отсутствующий орган воспалиться не может, поэтому это `stop`, а не вес. Сейчас таких правил восемь.

**Пол и возраст** задаются в самой статье: `sexOnly` (25 статей), `ageMin`/`ageMax` (11 статей). Внематочная беременность не покажется мужчине, височный артериит не всплывёт у тридцатилетнего.

## Подразумеваемые ответы

У раздела может быть поле `implies` — ответы, верные по определению. В разделе «потеря веса» человека не спрашивают, теряет ли он вес: `implies: {weight: "yes"}`. Движок подставляет их до расчёта и до проверки красных флагов.

## Слой красных флагов

`redflags.json` работает **поверх** разделов и не зависит от полноты списков. Если в дифференциальный ряд что-то не попало по недосмотру, сочетание признаков всё равно сработает.

Набор намеренно короткий (60 правил) и состоит из общеизвестных неотложных сочетаний — именно поэтому его можно составить надёжно, в отличие от длинных списков.

**Привязка к зонам.** У правила может быть поле `zones` — тогда оно работает только там. Это появилось после того, как правило про мгновенно возникшую боль со рвотой, написанное для головы, сработало на малярии.

**Возрастные границы.** `minAge`/`maxAge` нужны там, где состояние строго возрастное. Височный артериит практически не бывает до пятидесяти.

**Исключения.** Поле `unless` подавляет правило при определённых ответах. Слабость в обеих ногах — признак сдавления спинного мозга, но при стенозе позвоночного канала она проходит от наклона вперёд: это позиционная хромота, а не катастрофа.

## Слой якорей

`anatomy.json` связывает точку на модели тела с разделом. Координаты заданы **не в пикселях**, а в долях от габарита фигуры: `u` по горизонтали (0.5 — осевая линия), `v` по вертикали (0 — макушка, 1 — подошвы).

Чтобы прикрепить базу к новой модели, нужно заполнить четыре числа калибровки:

```
X = axisX + (u - 0.5) * 2 * halfWidth
Y = crownY + v * (soleY - crownY)
```

Все 52 участка пересчитываются формулой, ничего не перерисовывается руками.

У каждого участка есть ещё `landmark` — ориентир словами. Если пропорции новой модели отличаются, участок восстанавливается по нему.

**Стороны:** `right` и `left` — стороны тела, не экрана. На виде спереди правая сторона тела находится слева на экране.

Жалобы, которые нельзя показать пальцем — тошнота, головокружение, кровь в стуле — собраны в отдельный вход `symptom.list` без якоря.

## Статусы

`draft` → `compiled` → `reviewed` → `published`

Все 307 статей в статусе `compiled`: сверено с источником, врачом не проверялось. Это честно отражено в поле, и сайт должен показывать это состояние читателю.

## Что дальше

Наполнение завершено. Следующий шаг — сборка сайта:

- статический генератор, постоянный адрес у каждой статьи и каждого раздела
- алфавитный указатель и поиск по названиям и синонимам (`alt`, `aka`)
- модель тела с калибровкой по `anatomy.json`
- страница «что здесь не разбирают» и кнопка «моего случая здесь нет»
- вычитка формулировок юристом по медицинскому праву

Движок (`engine.js`) переносится на сайт как есть — он уже написан под это.
