# Заметки по коммитам

Короткие записи о том, что и почему менялось; полный лог реплик Claude Code — в `cc-chat-log.md`, отложенное — в `backlog.md`.

---

# Поиск: слова человека против названий справочника

Решение 10.09.2026. Задание передано Claude Code.

## Что было не так

Поиск в шапке и фильтр указателя сравнивали **подстроки**. Из 105 проверок на живом индексе прежний поиск проваливает **26**. Три показательных случая:

| Запрос | Что выдавал |
|---|---|
| «болит горло» | **ничего** — слова «болит» нет ни в одном названии, а «боль» подстрокой в «болит» не входит |
| «высокая температура» | «Длительная **не**высокая температура» — подстрока не знает границы слова |
| «больно наступать на пятку» | **ничего** — требовалось, чтобы подошли все слова, включая «наступать» |

Не находилось также: «кружится голова», «резко упало зрение», «все забываю», «першит в горле», «болит шея при повороте», «жёлтые глаза», «отдаёт в ногу из поясницы», «опухло колено», «чешется кожа», «задержка месячных», «болит мошонка», «постоянная слабость», «похудел без причины», «потею по ночам», «тревога и паника», «увеличены лимфоузлы».

Дефект тихий: строка «болит горло» — вероятно, самый частый запрос о здоровье вообще, и справочник на неё молчал при зелёной сборке.

## Что сделано

**1. Сравнение по основам слова.** Стеммер Портера для русского языка (алгоритм Snowball) написан прямо в `src/site/assets/poisk.js` — у проекта ноль зависимостей, и это не повод их заводить. «болит», «боли», «болью», «боль» дают одну основу «бол»; «высокая» и «невысокая» — разные, и подмена больше невозможна.

Совпадение основ: 2 — слово то же самое, 1 — одна основа начинается с другой и та не короче четырёх букв («голов» и «головн»). Четыре, а не три: на трёх «гор» связало бы «горло» с «горечью».

**2. Смягчено «подошли все слова».** Теперь — «подошло столько слов, сколько вообще удалось подобрать». Когда сходятся все, поведение прежнее; когда лишнее слово вроде «наступать» в справочнике не встречается, оно просто не отсекает всё подряд.

**3. Порядок выдачи не зависит от порядка слов.** «болит голова» и «голова болит» дают один список. Правила: сначала те, у кого слова запроса стоят в самом названии, а не в синонимах; потом разделы перед статьями (человек, набравший «кашель», ищет полку, а не редкую статью с этим словом в заголовке); потом точность совпадения; потом — у кого в названии меньше лишних слов.

**4. Указатель считает так же.** `search.js` берёт тот же разбор из `window.EZP`, который кладёт `poisk.js`. Иначе одна и та же строка в шапке находила бы раздел, а в указателе — ничего. Если разбора нет, остаётся прежнее сравнение подстрок: указатель обязан работать всегда.

**5. Недописанное последнее слово.** «пят» находит «Боль в пятке», «кров» — «Кровь в моче». Работало и раньше через подстроку, сохранено явным правилом.

## Границы, которые не двигаются

Поиск ведёт к **постоянному адресу раздела или статьи**. Это оглавление, а не разбор случая. Не появляется и не появится: подсказок «похоже, у вас…», подсчёта совпадений в процентах, разбора набранного текста как жалобы. Слово «симптом» в поиске не встречается ни в коде, ни в разметке.

Три прежних правила соблюдены без изменений: запрос не попадает в адрес (ни `?q=`, ни хеша, ни формы с `action`), ничего не сохраняется (ни localStorage, ни куки), поле создаёт скрипт — без JavaScript в шапке остаётся ссылка на указатель.

## Прогон

`tools/test-poisk.js` + `tools/poisk-cases.json`, добавлен в конец `npm test`. **101 запрос, 105 проверок.** Запросы написаны так, как их набирает человек, а не так, как называется раздел.

Поля случая: `top` — этот адрес обязан стоять первым; `in` — все перечисленные обязаны попасть в выдачу; `not` — ни один не должен. `not` существует ради «высокой температуры»: подмена словом-соседом обязана падать, а не просто выглядеть странно.

Прогон гоняет **ровно тот файл, который уезжает в браузер** — `poisk.js` экспортируется и в Node, и в `window`, тем же швом, что и движок ранжирования.

**Доказательство, что прогон не декоративный:** те же 105 проверок прогнаны против прежнего поиска — он валит 26. Это исполнение правила, выведенного из провала линта формулировок: всякая новая проверка заказывается вместе с прогоном, доказывающим, что она ловит то, ради чего написана.

Случаи правке под удобство не подлежат. Если прогон падает, чинить нужно поиск или синонимы `aka`/`alt` в базе, но не ожидание в файле.

## Цена

Файл в шапке вырос с 8,0 КБ до 20,5 КБ — стеммер и комментарии. Он висит на каждой из 510 страниц, поэтому цифра названа явно, а не спрятана. Индекс (77 КБ, 488 строк) по-прежнему грузится только по первому нажатию клавиши.

## Что осталось на потом

- «Боль в спине с нарушением мочеиспускания» выходит первой на запрос «болит спина» — это единственный раздел, у которого «спина» стоит в названии. Лечится не ранжированием, а синонимами `aka` у разделов о пояснице. Отдельная работа по данным.
- Запросы, которые не находятся из-за отсутствия слова в базе вообще («чешется», «немеют», «похудел» без второго слова), закрываются тем же способом: пополнением `aka` у разделов и `alt` у статей.
---

# Обиходные названия для трёх последних статей

10.09.2026. Поле `alt` отвечает за находимость: человек ищет «малиновый язык», а не «скарлатина». Из 364 статей поле было у 361; у краснухи, скарлатины и ангины при агранулоцитозе его не было. Проставлено из `tools/alt-fix.json`, в конец объекта статьи после `updated` — как у остальных.

Самих слов «краснуха» и «скарлатина» в списках нет намеренно: валидатор запрещает синоним, дословно повторяющий название статьи, — он ничего не добавляет поиску. У агранулоцитоза слово оставлено, потому что статья называется «Ангина при агранулоцитозе».

Итог: `alt` есть у всех 364 статей. Три новых синонима видны в индексе поиска в шапке; внутренних ссылок стало 23 930 вместо 23 920 — это строки «см.» в указателе.

---

# База знаний описана по факту

10.09.2026. `docs/knowledge-base.md` был единственным документом в репозитории, описывающим базу, и он устарел: 110 разделов, 307 статей, 252 случая, схема 1.0.0 «заморожена». На деле 124 раздела, 364 статьи, 327 виньеток, 301 случай ощущений, 101 запрос к поиску, схема 1.1.0, сайт из 510 страниц собран. Расхождение — 54 статьи и 14 разделов.

Заменён целиком. Каждое число в новом тексте сверено с выводом `validate`, `test` и `build` на текущем коммите, а те, что считались обходом `data/` (приоритеты разделов 55/53/15/1, `sexOnly` у 30 статей, `ageMin`/`ageMax` у 14, `stop`-правил 8, `focused`-разделов 10), пересчитаны заново и сошлись.

---

# Запреты на слова подняты на жёсткий уровень и охраняются в данных

10.09.2026. Пять правил линта — «ваши симптомы», «симптом», «анкет», «опрос», «диагностик» — лежали в списке SOFT, а SOFT проверяет только `title` и `description`. Слова, которые CLAUDE.md объявляет запрещёнными на экранах, в тексте страниц не проверялись вовсе. Перенесены в HARD: теперь 26 жёстких правил и 5 мягких.

По всему тексту 510 страниц нашлось пять совпадений. «Болезни без симптомов» на `/ukazatel/`, `/oblasti/gen/`, `/razdely/gen-silent/` — синоним раздела gen.silent, оставлен решением владельца; «Профилактика, диагностика и лечение COVID-19» — название документа Минздрава, оставлено. Оба разрешены точечно в `tools/lint-allow.json` (исключений стало 6). «Обычно опрос и осмотр» в поле `tests` физиологического тремора заменено на «расспрос»: речь о расспросе врача, а слово «опрос» на сайте занято под анкету.

Исключение на `/ukazatel/` грубое — это список из 488 строк, линт видит слово без окружения. Поэтому точная охрана поставлена в `tools/validate.js`: «симптом» запрещён во всех данных, кроме ровно строки «болезни без симптомов». Проверено подсадкой «симптомы на коже» в чужой `aka`: падает с адресом поля. «Бессимптомно» пропускается намеренно — в статьях об аневризме и фибрилляции это единственное точное слово; таких форм на сайте девять.

Вторая часть — слова, которых в базе не было: «отрыжка» вела в дивертикул Ценкера, «чёрные точки» в кариес, «чешется тело» и «ломит тело» в инородное тело дыхательных путей, «похудел», «озноб», «лысею», «светобоязнь» не находили ничего. Четырнадцати разделам добавлены 33 синонима `aka` из `tools/aka-add.json`; веса и списки состояний не тронуты, замок молчит. В `tools/poisk-cases.json` 120 запросов и 125 проверок вместо 101/105; на старых синонимах новые случаи падают в 15 местах, на новых — ни в одном. В задании число синонимов названо 38, в файле их 33 — по счёту строк и по диффу.

---

# Вычитка, заход 1: пятнадцать статей

10.09.2026. Первая партия правок по итогам сплошной вычитки `detail`. Массив заменяется на месте, порядок полей статьи не меняется, дифф точечный: 8 файлов, 36 вставок и 8 удалений — восемь поправленных абзацев и семь новых блоков. Данные в `tools/detail-vych-1.json`.

Что исправлено. Кишечная непроходимость была единственной из 47 статей с красной плашкой без прямого указания обратиться за помощью — добавлен последний блок «Что делать». Панической атаке и СДВГ добавлена строка кризисной помощи, которая у шести соседей по разделу уже была. У пульпита убран единственный на базу пункт «принять обезболивающее, разрешённое вам врачом»: оговорка не спасает, сайт всё равно советовал принять; линт этого не ловит, потому что знает «принимайте», а не «принять» — правило пока не дописывается, чтобы не уронить два десятка законных запретов. Статье о шейном лимфоузле, работающей в разделах о груди и пахе, добавлен блок «Узлы не только на шее». Пяти статьям, открывающим разделы, добавлено по фразе с местом, о котором раздел («вокруг пупка», «в виске», «в левое подреберье», «под ребром»). Пять самых коротких неотложных — отслойка плаценты, эпиглоттит, паратонзиллярный абсцесс, агранулоцитоз, дифтерия — получили по блоку различения; стало 5 блоков и 303–337 слов.

Замок молчит, валидатор, четыре прогона и сборка зелёные, 364 статьи с `detail` и 47 с плашкой — как было.

---

# Вычитка, заход 2: десять неотложных дополнены

10.09.2026. Из 47 статей с красной плашкой пятнадцать были короче 330 слов; пять самых коротких вошли в первый заход, остальные десять — сюда. Каждой добавлен один блок, отвечающий на вопрос, который у читателя возникал и оставался без ответа: перекрут яичника — «Кто в опасности» (в том числе девочки-подростки при нормальном яичнике); ущемлённая грыжа — «С чем путают» (бедренная грыжа, которая меньше и ущемляется чаще); камень в протоке — чем отличается от обычной желчной колики; малярия — что сказать врачу и почему это решает; угарный газ — у кого протекает тяжелее; сердечный обморок — что рассказать врачу об эпизоде; разрыв селезёнки — почему после мононуклеоза запрещают спорт; ангина Людвига — как до этого не доводить; химический ожог глотки — что бывает через недели; преэклампсия — что это значит для дальнейшей жизни.

Данные в `tools/detail-vych-2.json`, замена массива на месте: 6 файлов, 40 вставок, ни одного удаления — ни один существующий абзац не переписан. Блок с указанием к действию везде остался последним или предпоследним. Стало 5–6 блоков и 362–406 слов на статью. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было. Партии не пересекаются ни одной статьёй.

---

# Вычитка, заход 3: оборот про обезболивание и двенадцать различений

10.09.2026. Две части, 19 статей, данные в `tools/detail-vych-3.json`, замена массива на месте: 7 файлов, 55 вставок и 7 удалений.

Первая часть — единый оборот. Слово «обезболивание» встречается в базе 84 раза, и почти везде оно стоит правильно: описывает, что делает врач, или объясняет механизм. Но в семи статьях — мононуклеоз, скарлатина, вирусный фарингит, растяжение голеностопа, паротит, эпидидимит, ПМС — оно стояло голым пунктом в перечне домашних мер, тогда как в двух десятках соседних статей тот же перечень оговорён словами «по назначению врача». Правила это не нарушало, но разнобой в такой вещи выглядел случайностью. Во всех семи добавлено «по назначению врача»; у фарингита и растяжения оно заменило «по необходимости». Проверено пословно: других изменений в этих абзацах нет.

Вторая часть — первая партия из полосы статей, работающих в двух-трёх разделах. Двенадцати добавлено по блоку различения или ответа на вопрос, который оставался без ответа: постназальный затёк — кашель, который тянется месяцами; киста Бейкера — что за ней стоит в колене и почему у ребёнка это другое; перикоронит — почему зуб мудрости предлагают удалить; лямблиоз — почему после лечения жалобы иногда остаются; вросший ноготь — что делают в кабинете; локтевой канал, рожа, перикардит, миокардит — с чем путают; сиалоаденит — как уменьшить вероятность повторения; плевральный выпот — по чему судят о причине; полипы носа — чем отличается от обычной заложенности. Стало 5–6 блоков, 309–408 слов.

Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было. С партиями 1 и 2 пересечений нет.

---

# Слово «обезболивание» убрано из перечней домашних мер

10.09.2026. Поправка поверх третьего захода. Оговорка «по назначению врача», дописанная в семи статьях, владельцем признана недостаточной: у человека может быть непереносимость того, что входит в состав, или он выпьет лишнего — а пункт в перечне рядом с «холодом» и «покоем» читается как совет независимо от оговорки. Решение жёстче: слову «обезболивание» в перечне того, что делать дома, не место вовсе. Замена не называет ни средства, ни группы: «Чем облегчить боль, подбирает врач», при температуре — «Чем облегчать боль и высокую температуру, решает врач».

Правка охватила 38 абзацев в 37 статьях, `tools/detail-vych-3.json` перезаписан (47 статей, двенадцать дополненных блоков прежние). Дифф строго парный: 8 файлов, 38 вставок и 38 удалений, новых блоков нет. Сознательно остаются три класса: запреты («не принимать обезболивающее до осмотра»), предупреждения о вреде (лекарственная головная боль, язва, непереносимость при полипах и астме) и то, что делают в стационаре или назначает врач как действующее лицо.

Числа, разошедшиеся с заданием: снято 39 вхождений слова, а не 37 (у герпангины в одном абзаце их было два); в `conditions-*.json` их было 115 и осталось 76, из них 64 в `detail`. Контрольный скрипт из задания выводит не 0, а 7 — все семь из разрешённого класса «что назначает врач» (анальная трещина, рак поджелудочной, грушевидная мышца, миелома, плеврит, грудной миофасциальный синдром) и предупреждение о частом приёме при мигрени; его список исключений не знает порядка слов «врач назначает» и формы «обезболивают». Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Единообразие: там, где обезболивание назначает врач, названа цель

10.09.2026. Добивка к решению про слово «обезболивание». В прошлой партии оно убрано из перечней домашних мер, а предложения вида «врач назначает обезболивание» оставлены как описание того, что делает врач. Формально верно, но одинаковые фразы в соседних статьях — мышечная боль в шее и в грудном отделе — стали выглядеть по-разному, и объяснить это читателю нечем. Там, где слово называет то, что даёт врач, оно заменено на цель — «облегчение боли», «подбирает, чем облегчить боль». Четырнадцать статей, `tools/detail-vych-4.json`, только словесная правка: 4 файла, 14 вставок и 14 удалений, ни одного нового блока, снято 15 вхождений.

В `detail` слово остаётся в 47 предложениях, и все законны: 3 — «под местным обезболиванием» как укол перед вмешательством, 44 — запреты («не принимать обезболивающее до осмотра») и предупреждения о вреде (язва и почки от регулярного приёма, правило «не чаще двух дней в неделю», непереносимость при полипах и астме, абузусная головная боль). Контрольный скрипт из задания вывел ровно 47, 3 и 44. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 5: двенадцать статей дополнены

10.09.2026. Продолжение полосы статей, работающих в двух-трёх разделах. Правок существующего текста нет вовсе — только новые блоки, по одному на статью, и каждый отвечает на вопрос, который у читателя возникает и остаётся без ответа: сухое раздражение глотки — отчего слизистая сохнет незаметно (дыхание ртом во сне, кондиционер, ингалятор без полоскания, средства с сухостью во рту в побочных); кандидоз рта — с чем путают белый налёт; туберкулёзный лимфаденит — заразен ли человек; астения после инфекции — чем отличается от подавленности (ключ в желании); запястный канал — три случая, которые ведут иначе; вирусный фарингит — сколько это длится и что считать нормой; ахалазия — почему до диагноза проходят годы; ком в горле — почему от проверок становится хуже; гломерулонефрит — чем отличается от инфекции мочевых путей; анальная трещина — почему заживление срывается; шейный остеохондроз — от шеи ли головокружение и шум в ушах; миелома — найденный белок ещё не миелома.

Данные в `tools/detail-vych-5.json`, замена массива на месте: 6 файлов, 48 вставок, ни одного удаления. Стало 5 блоков и 342–380 слов. Четыре статьи из этой партии — фарингит, трещина, шейный отдел, миелома — уже правились в заходах 3 и 4; файл несёт эти правки, поэтому дифф остался чисто добавочным. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 6: двенадцать статей дополнены

10.09.2026. Правок существующего текста нет — только новые блоки, по одному на статью: судороги ног — у беременных и у пожилых две отдельные истории, и отёк одной голени при беременности не судорога; стрептококковая ангина — нужно ли удалять миндалины, с общепринятыми ориентирами по числу подтверждённых ангин; меланома — что чаще всего оказывается неопасным (себорейный кератоз, гемангиома, подногтевое кровоизлияние); ахиллово сухожилие — отдельно про боль у самой пятки, где упражнения другие; паротит — взрослому, который не помнит прививок; рак мочевого пузыря — отчего ещё бывает кровь в моче и почему правило не меняется; миастения — почему диагноз ставят поздно, с советом снять лицо и голос на телефон утром и вечером; паралич Белла — что бывает при восстановлении; артроз тазобедренного сустава — про замену сустава, чего ждать; целиакия — не путать с чувствительностью к глютену; грудной остеохондроз — здесь же прячется остеопороз; диабетическая язва стопы — ежедневные правила, которые защищают.

Данные в `tools/detail-vych-6.json`, замена массива на месте: 6 файлов, 48 вставок, ни одного удаления. Стало 5 блоков и 381–431 слово. Паротит, тазобедренный сустав и грудной отдел уже правились в заходе 3; файл несёт те правки, дифф чисто добавочный. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 7: двенадцать статей дополнены

10.09.2026. Только новые блоки, по одному на статью: вирусный гепатит — как не заразиться и что делать после контакта (бытового пути нет, изолировать дома не нужно; при уколе чужой иглой обращаться в первые часы); кластерная головная боль — что бывает между сериями; лямблиоз — откуда чаще всего берётся (вода, купание, детские коллективы; кошки и собаки источником для человека практически не бывают); киста молочной железы — чем отличается от фиброаденомы; эпителиальный копчиковый ход — почему возвращается после операции; опухоль носа и пазух — с чем её путают (у ребёнка односторонняя заложенность со зловонным отделяемым почти всегда инородное тело); себорейный кератоз — чем отличается от других тёмных пятен; рак эндометрия — что ещё вызывает кровотечение после менопаузы; синдром Мэллори-Вейсса — с чем путают кровь в рвоте; болезнь кошачьих царапин — кому опасна и надо ли расставаться с кошкой; мастоидит — отдельно про стёртое течение; препателлярный бурсит — как защитить колени на работе.

Данные в `tools/detail-vych-7.json`, замена массива на месте: 7 файлов, 48 вставок, ни одного удаления. Стало 5–6 блоков и 316–427 слов. Лямблиоз и препателлярный бурсит правились в заходе 3; файл несёт те правки, дифф чисто добавочный. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 8: двенадцать статей дополнены — что человек делает сам

10.09.2026. Заход получился в основном про то, что человек делает сам, — именно здесь справочник может дать больше всего, не выходя за свои границы. Только новые блоки, по одному на статью: бронхоэктазы — как очищать бронхи дома (когда, сколько, в каком положении и как дышать); гинекомастия — подростку и его родителям (встречается более чем у половины мальчиков и проходит само; спортивное питание усугубляет); наружный отит — почему ушам вредит чистка; неврит зрительного нерва — что это значит для будущего и почему томографию делают, даже когда зрение вернулось; медикаментозный ринит — что чувствует человек в первые дни отмены, по дням; гиперактивный пузырь — ночные подъёмы не всегда пузырь; дивертикул Ценкера — почему его долго не находят; спленомегалия — что нельзя делать при увеличенной селезёнке; увеит — почему ищут причину, даже когда глаз прошёл; беспокойные ноги — что усиливает синдром незаметно; искривление перегородки — чего от операции ждать не стоит; кокцигодиния — как сидеть, пока болит.

Данные в `tools/detail-vych-8.json`, замена массива на месте: 6 файлов, 48 вставок, ни одного удаления. Стало 5 блоков и 331–359 слов. Наружный отит и кокцигодиния правились в заходе 3; файл несёт те правки, дифф чисто добавочный. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 10: двадцать две статьи дополнены

10.09.2026. Партия крупнее прежних — владелец спросил, почему по двенадцать; дальше партии такого размера или больше. Только новые блоки, по одному на статью, и заход в основном про то, что человек делает сам и как читать заключения: контактный дерматит — как самому найти виновника; базалиома — защита от солнца, которая действительно работает; евстахиит — как продувать уши в самолёте; дисфункция голосовых складок — что делать в момент приступа; функциональная диспепсия — как вести дневник питания; разрыв мениска — что значит заключение томографии; острый ларингит — если голос нужен по работе; узел щитовидной железы — что значат цифры в заключении УЗИ; наджелудочковая тахикардия — как записать приступ; сотрясение — возвращение к делам ступенями; вестибулярный нейронит — почему нельзя долго лежать; инородное тело глотки — отдельно про рыбную кость; ревматическая полимиалгия — с чем путают; аксиальный спондилоартрит — гимнастика, которая здесь именно лечит; эпидидимит — почему обследуют и лечат партнёра; артроз кистей — приёмы, которые облегчают день; скарлатина — когда в школу и что делать с домашними; Рейно — что провоцирует незаметно; рак поджелудочной — кому имеет смысл наблюдение; поверхностный тромбофлебит — что делать, пока ждёте приёма; коклюш — как защитить младенца; Меньер — как жить между приступами.

Данные в `tools/detail-vych-10.json`, замена массива на месте: 8 файлов, 88 вставок, ни одного удаления. Все двадцать две теперь 361–414 слов. Эпидидимит, артроз кистей, скарлатина и рак поджелудочной правились в заходах 3 и 4; файл несёт те правки, дифф чисто добавочный. Заход 9 в этот репозиторий не поступал — нумерация партий владельца, а не пропуск в применении. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 9: двенадцать статей дополнены

10.09.2026. Пришёл после десятого; порядок роли не играет, партии не пересекаются. Только новые блоки, по одному на статью: перелом луча — что сделать до приёмного отделения (первым делом снять кольца, пока отёк не нарос); разрыв передней крестообразной связки — что такое реабилитация и сколько она длится; эозинофильный эзофагит — как понять, что вы приспособились; растяжение голеностопа — когда возвращаться к нагрузке, по пробам, а не по календарю; ортостатическая гипотензия — почему это опаснее, чем кажется; подострый тиреоидит — что происходит дальше, по месяцам; блефарит — почему это возвращается; рак шейки матки — что означает найденный вирус; пародонтит — почему кровоточивость не повод чистить мягче; сухой глаз — что незаметно сушит глаза; рак гортани — что чаще всего оказывается причиной охриплости; острая кривошея — как спать и работать, пока не отпустило.

Данные в `tools/detail-vych-9.json`, замена массива на месте: 4 файла, 48 вставок, ни одного удаления. Стало 5 блоков и 346–383 слова. Голеностоп и кривошея правились в заходе 3; файл несёт те правки, дифф чисто добавочный. По протоколу владельца: при самопроверке этой партии починенный линт поймал «у вас» в новом блоке про эзофагит, фраза переписана до отправки. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 11: двадцать три статьи

10.09.2026. Двадцать одна статья дополнена блоком, двум дописано по фразе. Блоки — про то, как делать то, что советуют, и как не бросить на полпути: кератит — правила для тех, кто носит линзы; выкидыш — что происходит с телом дальше; герпес на губах — кого нужно поберечь; бессонница — что делать ночью, когда не спится; мастит — как предупредить застой и трещины; боррелиоз — чего боррелиозом не является; инфекционный эндокардит — кому нужна защита перед стоматологом; рассеянный склероз — что помогает, кроме лекарств; венозная язва — про компрессию, как носить, чтобы работало; рак молочной железы — как знать свою грудь; токсоплазмоз — что значат антитела в анализе; поясничный остеохондроз — что делать в первые дни обострения; подошвенный фасциит — растяжка, которая работает, как именно; чесотка — обработка дома, что нужно, а что лишнее; манжета плеча — что делать и чего избегать, пока болит; апноэ сна — если аппарат не даётся; вазовагальный обморок — как пережить укол и осмотр; бранхиогенная киста — что ещё бывает сбоку на шее; кровоизлияние под конъюнктиву — с чем не надо путать; локтевой бурсит — как не получить снова; герпангина — как напоить ребёнка, который отказывается.

Эпиглоттит и паратонзиллярный абсцесс не дотягивали до коридора на двенадцать и девять слов; неотложные статьи намеренно короче, поэтому вместо блока к последнему абзацу каждой дописана фраза: скорую вызывают, даже если до больницы близко; курс после вскрытия абсцесса доводят до конца. Проверено: обе фразы строго продолжают прежний абзац.

Данные в `tools/detail-vych-11.json`: 8 файлов, 86 вставок и 2 удаления — ровно те два абзаца. Стало 322–432 слова. Семь статей уже правились в заходах 1 и 3; файл несёт те правки. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 12: двадцать три статьи дополнены

10.09.2026. Только новые блоки, по одному на статью — как делать, что не работает и что не значит болезни: де Кервен — молодой матери отдельно; гигрома — почему то появляется, то исчезает; помутнения стекловидного тела — как проверить себя дома; глоссит — что на языке бывает и не значит болезни; серная пробка — кому пробки образуются постоянно; Жильбер — находка в анализе и что с ней делать; гидроцеле — у младенца, что важно родителям; заеды — почему не заживает неделями; розовый лишай — что можно и чего нельзя, пока сыпь; халязион — как делать тёплый компресс правильно; рак почки — что значит «киста почки» в заключении; лейкоплакия рта — как осматривать рот самому; неврома Мортона — обувь как лечение; баланопостит — отдельно про мальчиков; вальгус — что продают и что из этого работает; полип кишки — про колоноскопию, то, что мешает на неё пойти; онихомикоз — обувь, дом и как не заразиться снова; стриктура пищевода — как есть, пока сужение не расширили; аденоиды — дыхание ртом, почему это не мелочь; внезапная тугоухость — простая проверка, которая помогает решиться; гнёздная алопеция — что не работает; энтеровирусная экзантема — когда в сад и что с ногтями; окклюзия артерии сетчатки — почему после этого обследуют сердце и сосуды шеи.

Данные в `tools/detail-vych-12.json`: 7 файлов, 92 вставки, ни одного удаления. Де Кервен, Мортон, вальгус и экзантема правились в заходе 3; файл несёт те правки. По протоколу владельца: его проверка на посторонние символы поймала «кista» латиницей в статье про почку, исправлено до отправки; здесь новые блоки прогнаны на смешение алфавитов — чисто. Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Вычитка, заход 13: последние шестьдесят восемь статей — коридор длины закрыт

10.09.2026. Последняя партия сплошной вычитки: 68 статей — все, что оставались короче коридора. Каждая дополнена одним новым блоком, правок существующего текста нет. Данные в `tools/detail-vych-13.json` (368 407 байт, 1502 строки, LF): 8 файлов, 272 вставки, ни одного удаления. Восемь статей уже правились в заходах 1 и 3; файл несёт те правки, дифф чисто добавочный. Новые блоки прогнаны на смешение алфавитов — чисто.

По протоколу владельца: предпроверка партии поймала «повторять каждые три-четыре дня в течение двух недель» у педикулёза — запрет DETAIL_FORBIDDEN на длительность курса; речь шла о вычёсывании, но правило не различает, и правильно: переписано на признак окончания — «пока за два прочёсывания подряд на салфетке не окажется ни одной живой вши».

Длина после партии, считая заголовки и абзацы по пробелам: средняя 409 слов, самая короткая 330 (testicular.torsion), короче 330 — ноль. Заодно закрыт вопрос про расхождение средних длин в прошлых заметках: владелец считал заголовки вместе с абзацами, здесь считались только абзацы; при одном способе счёта ряды совпадают.

**Вычитка завершена.** Тринадцать заходов, 364 статьи из 364 имеют подробный текст, ни одна не короче 330 слов, 47 неотложных несут красную плашку. Замок молчит, валидатор («Ошибок нет», 364 статьи, 124 раздела), четыре прогона (виньетки 327 из 327, ощущения 301 из 301, поиск 125 проверок) и сборка (510 страниц, «Формулировки чистые») зелёные.

---

# Профильная вменяемость, заход 1: тридцать статей отвечают тому, кто пришёл из своего раздела

10.09.2026. Новая полоса работы после вычитки. Вопрос один: отвечает ли статья тому, кто пришёл в неё из своего раздела. Проверены все 1207 связей «раздел → состояние» — называет ли статья место на теле или саму жалобу, с которой человек к ней пришёл. Нашлось 52 пары без единого слова раздела; каждая прочитана глазами, четыре предполагавшихся расхождения оказались промахами стеммера на словоформах и не правились. Списки состояний под замком и не трогались — правились статьи.

Три группы правок. Односторонность и «бок» — почечная колика, пиелонефрит, опоясывающий лишай, запор, СРК, ВЗК, рак кишки, цистит стоят в разделах про правый и левый бок и о боку не говорили. Сустав, о котором раздел, не назван — подагра, ревматоидный артрит, септический артрит, шейный корешок, артроз колена, грыжа диска, тромбофлебит. Жалоба, с которой пришли, не описана — тиреотоксикоз (сбой цикла, приступы как панические атаки), фибрилляция (как отличить от паники), ВИЧ (понос неделями), гипотиреоз (ногти), инфаркт (отдача в челюсть как зубная боль), инсульт (внезапная потеря слуха на одно ухо), кандидоз, язва, абузусная боль, полипы носа, рак ротоглотки, перелом ребра, гигантоклеточный артериит, субдуральная гематома. Диабетической нейропатии добавлен блок про автономную форму.

Данные в `tools/detail-profil-1.json`: 8 файлов, 33 вставки и 29 удалений — 29 заменённых абзацев и один новый блок, заголовки не тронуты. Машинный прогон по 1207 парам: без единого слова раздела 52 → 18, первых по частоте 1 → 0, в первой тройке 6 → 1. Четыре пары текстом не лечатся и переданы владельцу как вопрос к спискам кандидатов (локоть → артроз кисти; мошонка → перелом ребра; выпадение волос → рак кишки; отёки → анемия). Замок молчит, валидатор, четыре прогона и сборка зелёные, 364/47 — как было.

---

# Профильная вменяемость, заход 2: четырнадцать статей после сплошного чтения всех 364

10.09.2026. Первый заход был машинным — ловил пары, где статья не называет ни места, ни жалобы. Второй сделан чтением: прочитаны все 364 статьи, каждая вместе со списком разделов, из которых в неё попадают, с одним вопросом — отвечает ли она тому, кто пришёл с этой жалобой. Читалось по зонам. Находок немного, база написана крепко; исправлено четырнадцать блоков: рёберно-хрящевая боль у женщин ощущается как боль в молочной железе; при носовом кровотечении связь с давлением обратная — кровь и испуг поднимают цифры; прожилки крови при надсадном кашле и откуда они; нехватка железа усиливает беспокойные ноги; ночные подъёмы при аденоме дробят сон; паховая грыжа тянет в бедро; эндометриоз и сам цикл; цистит и задержка мочи при сгустках; узел в подмышке при лимфоме принимают за уплотнение в груди; аллергический отёк давит там же, где пазухи; кровь носом у детей при ОРВИ; левое подреберье при стенокардии; боль при эмболии сбоку и под рёбрами; кишечник при автономной нейропатии.

Проверялись и оказались чистыми: гипергидроз в «Ночной потливости», гиперактивный пузырь в «Жажде», лимфаденит в «Уплотнении в молочной железе», артроз тазобедренного сустава в «Боли в колене». Машинная сеть после двух заходов: пар без единого слова раздела 52 → 17, первых по частоте 1 → 0, в первой тройке 6 → 1 (наружный отит в «Боли в ухе», где не хватает просторечия «стреляет»). К списку пар, которые текстом не чинятся, добавлены две: кровь в мокроте → цирроз, жажда → анемия.

Данные в `tools/detail-profil-2.json`, применены поверх первого захода (две статьи общие): 7 файлов, 14 вставок и 14 удалений, заголовки не тронуты. Замок молчит, валидатор, четыре прогона (первым 303, в тройке 321 — как было) и сборка зелёные, 364/47. **Сплошное чтение 364 статей по разделам завершено — это была последняя крупная работа по содержанию из плана.** Отдельная находка на будущее: у 49 статей поле «Что это» разрослось до 70–121 слова при медиане 34 и наполовину повторяет подробную статью — решение владельца отдельным заданием.

---

# Поле «Что это»: строка в выдаче перестала обрываться на полумысли

10.09.2026. Строка под названием состояния в разделе и description страницы строятся из первой фразы поля `what`. У 46 статей эта фраза была длиннее 110 знаков, и строка обрывалась многоточием посреди мысли («температура подскакивает до 39 и выше за считаные часы, сразу…»). Ещё у 49 поле разрослось до 70–121 слова при медиане по базе 34 и наполовину дословно повторяло подробную статью тут же ниже — человек читал одно и то же дважды. Объединение списков — 74 статьи, плюс рак молочной железы и рак поджелудочной, у которых первой фразой стояло безликое и одинаковое «Опухоль ткани железы.».

76 полей переписаны по одному правилу: первая фраза — законченная мысль в пределах 110 знаков, всё поле — принятые по базе 30–40 слов, вынутое целиком есть в подробной статье. Применено скриптом из корня репозитория, который вместе с файлом данных после применения удалён: 9 файлов, 76 вставок и 76 удалений, ни строки сверх. Меняется ровно одно поле; веса, базовые частоты, стоп-факторы, `detail`, `alt`, `emergency` не тронуты.

Итог по базе: обрезанных строк выдачи 46 → 0, совпадающих начал 1 → 0; поле «Что это» — медиана 33 слова, среднее 34, максимум 70; первая фраза — медиана 52 знака, максимум 107. По 76 правленым: поле 72 → 35 слов, первая фраза 134 → 64 знака. Предполётная проверка владельца поймала у бессонницы «на протяжении трёх месяцев» — запрет на длительность курса, переписано; у эпилепсии — дословный повтор фразы из `detail`, переписано. Замок молчит, валидатор (364 статьи, 1207 связей), четыре прогона и сборка зелёные, 364/47.

---

# Шесть пар, объявленных не чинимыми текстом, всё же чинятся текстом

10.09.2026. По итогам профильной вменяемости шесть связей «раздел → состояние» были переданы владельцу как вопрос к спискам кандидатов: артроз кисти в «Боли в локте», перелом ребра в «Боли в мошонке», опухоль толстой кишки в «Выпадении волос», анемия в «Отёках ног» и в «Жажде», цирроз в «Крови в мокроте». Разбор показал, что вывод был поспешным: за каждой парой стоит настоящая связь или настоящая путаница, и обе описываются в статье, не трогая списки.

Правлены пять статей, по одному абзацу. Локоть артрозом поражается редко и обычно после перелома или тяжёлого ручного труда — читателя разворачивает к надмыщелиту и кубитальному каналу, которые стоят в том же разделе выше. Нижние рёбра прикрывают почку, а боль от почки отдаёт вниз, в пах и мошонку, тем же путём, что при колике. Опухоль правых отделов кишки годами проявляется только железодефицитной анемией, а та даёт ломкость ногтей и выпадение волос — цепочка была в базе, но разложена по двум статьям. Анемия ни отёков, ни жажды не вызывает, и сказано прямо: отёки уводят к сердцу, почкам и щитовидной железе, жажда с обильным мочеиспусканием — к сахару крови, а найденный заодно низкий гемоглобин объясняет усталость, но не отменяет второго поиска. Кровь при циррозе идёт из вен пищевода, во рту вызывает кашель и потому принимается за кровь при кашле; для человека с циррозом различение источника ничего не меняет в действии.

Данные в `tools/detail-shest.json`, применены скриптом из корня, который после применения удалён: 5 файлов, 5 вставок и 5 удалений, заголовки не тронуты, прибавка около 187 слов. Прежние предложения сохранены дословно — от 80 до 100 процентов на абзац. Пар без единого слова раздела 17 → 13, все шесть целевых закрыты. Списки кандидатов, веса, базовые частоты и стоп-факторы не тронуты, замок молчит; валидатор (364 статьи, 124 раздела, 1207 связей), четыре прогона и сборка зелёные, 364/47.

---

# Поле «Что это»: первая фраза приведена к настоящему пределу — 90 знаков

10.09.2026. Строку под названием состояния режут два разных места, и пределы у них разные: список раздела в HTML обрезает на 90 знаках (`src/site/pages/syndrome.js`, константа `GIST`), данные уточнения — на 110 (`src/site/payload.js`). В прошлом заходе поля правились под 110 и проверялись по данным, поэтому в HTML обрывы остались: 62 строки из 1207, у 24 статей. Именно эту страницу человек видит сразу, и она же единственная работает без JavaScript.

Константа 90 не тронута: объяснение при ней верное — иначе список выходит рваный, у одной статьи шесть слов, у соседней сорок. Правился текст. У 24 статей первая фраза укорочена и осталась законченной мыслью, вынесенное переехало во вторую фразу того же поля; шесть из них — из прошлого захода, остальные восемнадцать были длинными и раньше, просто отбирались по неверному пределу.

Данные в `tools/detail-what-90.json`, применены скриптом из корня, который после применения удалён: 9 файлов, 24 вставки и 24 удаления, меняется только поле `what`. Проверка теперь по собранному сайту, а не по данным: обрезанных строк 0 и в HTML раздела, и в данных уточнения, обе по 1207. По базе первая фраза — медиана 52 знака, максимум ровно 90, длиннее нет; совпадающих начал нет; поле — медиана 33 слова. Предполётная проверка владельца поймала у клостридиального колита «во время курса антибиотиков или в течение двух месяцев после него» — сразу два запрета, курс средств и длительность; переписано на «во время лечения антибактериальными средствами и в ближайшие месяцы после него». Прежнее поле валидатор пропускал: `DETAIL_FORBIDDEN` к `what` не применяется, только `TREATMENT`. Замок молчит, валидатор (364 статьи, 124 раздела, 1207 связей), четыре прогона и сборка зелёные, 364/47.

---

# Поле updated: честные даты вместо 27 августа

10.09.2026. Нашлось при подготовке переотправки карты сайта поисковикам. 307 статей из 364 стояли с датой 27.08 — то есть раньше, чем была написана хоть одна подробная статья. За сентябрь эти статьи получили подробный текст, обиходные синонимы, сплошную вычитку, профильную правку и переписанное поле «Что это»; изменилось в них практически всё, а поле говорило, что не менялось ничего. Бьёт это по двум местам: читателю на переписанной вчера статье показывалась августовская дата, а из того же поля строится `<lastmod>` в `sitemap.xml`, по которому поисковик решает, что перечитывать. Разделы в том же положении — 110 из 124, — и два файла данных: `anatomy.json` правился 06.09, `redflags.json` 05.09, оба стояли с 27.08.

Ставить всем сегодняшнее число было нельзя: это та же ложь, только свежая, а доверие Google к `lastmod` устроено двоично — от систематически неверных дат он перестаёт пользоваться картой целиком. Дата вычислена по истории репозитория: последний коммит, в котором объект действительно изменился, причём само поле `updated` из сравнения исключено, иначе правка даты считалась бы изменением содержания. Я пересчитал все 478 дат независимо тем же методом, пройдя коммиты по `data/conditions-*.json` и `data/syndromes-*.json`: сошлись все до одной, обе даты файлов совпали с последним коммитом, тронувшим файл.

Данные в `tools/dates-fix.json`, применены скриптом из корня, который после применения удалён: 23 файла, 481 вставка и 481 удаление. Проверено программно, что во всех 23 файлах изменилось только поле `updated` — сравнение объектов с вырезанным полем совпадает дословно. У `redflags.json` две строки вместо одной: файл был без перевода строки в конце, скрипт привёл его к общему виду. Стало: статьи 09.09 — 72, 10.09 — 292; разделы с 02.09 по 10.09; в карте не осталось ни одной страницы старше 1 сентября. Замок молчит — `updated` в отпечаток не входит; валидатор (364 статьи, 124 раздела, 1207 связей), четыре прогона и сборка зелёные, 364/47.

Одно число в задании разошлось с фактом: страниц с датой старше 01.09 в карте было не 333, а 431. Помимо 307 статей и 110 разделов туда входили 14 страниц, берущих дату из `anatomy.json` и `redflags.json`, — десять зон, указатель областей, список жалоб и две страницы из правил тревог. Оба файла в партии есть, поэтому итог тот, что и обещан: ноль.

---

# Источники: три адреса вели не туда, 112 подписей заменены на точные

10.09.2026. Продолжение отчёта `tools/istochniki-proverka.md`: из этой сессии сеть закрыта, и проверку 163 адресов рубрикатора Минздрава довели с другой стороны, через браузер владельца. Нашёлся программный интерфейс рубрикатора, который на живой документ отвечает кодом 200 и отдаёт название, а на несуществующий — кодом 400; различение подтверждено на выдуманном номере и на живом номере с выдуманной версией. Прогнаны все 163 адреса: **мёртвых нет ни одного**. Это хорошая новость, и она же — ответ на вопрос, ради которого затевалась проверка.

Но нашлось другое. Три адреса вели на чужой документ. У воспалительных заболеваний кишечника ссылка «Язвенный колит» указывала на «Болезнь Крона» — теперь статья честно ссылается на оба документа, каждый по своему адресу. У отёка Квинке ссылка «Анафилактический шок» указывала на «Крапивницу», ошибка на единицу в номере. У себорейного кератоза стоял документ о доброкачественных новообразованиях яичников. Все три заменены и проверены тем же интерфейсом.

Заодно 112 подписей заменены на точные названия документов, взятые из ответа рубрикатора. Осторожная формула «Справочные материалы по КР по такой-то теме» была уместна, пока названия не были известны; теперь читателю честнее видеть, на какой именно документ ссылается статья. Починены и шесть подписей с потерянной закрывающей кавычкой. Все 165 подписей теперь одной формы: `КР Минздрава РФ «точное название»`.

Данные в `tools/sources-fix.json`, применены скриптом из корня, который после удалён: 9 файлов, 115 вставок и 115 удалений. Проверено программно, что во всех девяти изменилось только поле `sources`; ни у одной статьи не изменилось число источников. Ссылок по базе 205, разных подписей 165, уникальных адресов стало 165 вместо 163. Статьи без ссылки на рубрикатор — их 161, там источник указан национальным руководством — не тронуты вовсе. Замок молчит, валидатор (364 статьи, 124 раздела, 1207 связей), четыре прогона и сборка зелёные, 364/47.

Одно число в задании разошлось: контрольная проверка обещала «ссылок 224», на деле их 205 — и было 205 до партии. Разных подписей ровно 165, как и обещано.
